import { Layout } from '../../components';
import './Pos.css';

interface PosProps { }

export const Pos: React.FC<PosProps> = () => {
    return (
        <Layout>
            <div className='pos-wrapper'>

            </div>
        </Layout>
    );
}